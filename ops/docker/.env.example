# ==========================================
# EasyCloudPan 环境配置
# ==========================================
# 使用说明：
# 1. 复制此文件为 .env（如果不存在，setup.bat 会自动创建）
# 2. 根据下方说明修改必要配置
# 3. 标记 [必改] 的配置需要在生产环境修改
# 4. 标记 [可选] 的配置可以使用默认值
# ==========================================

# ------------------------------------------
# 数据库配置 (PostgreSQL) [可选]
# ------------------------------------------
# 本地开发可使用默认值，生产环境建议修改密码
POSTGRES_DB=easypan
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456

# ------------------------------------------
# Redis 配置 [可选]
# ------------------------------------------
# 本地开发可使用默认值，生产环境建议修改密码
REDIS_PASSWORD=password123

# ------------------------------------------
# MinIO 配置 (对象存储) [可选]
# ------------------------------------------
# 本地开发可使用默认值
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadminpassword
MINIO_BUCKET=easypan
MINIO_BUCKET_NAME=easypan

# ------------------------------------------
# 后端应用配置 [可选]
# ------------------------------------------
SERVER_PORT=7090
# PROJECT_FOLDER: 文件存储根目录 [必改]
# 作用: 存储所有用户文件、头像和临时文件
# 目录结构:
#   ${PROJECT_FOLDER}/file/   -> 存储用户上传的文件
#   ${PROJECT_FOLDER}/avatar/ -> 存储用户头像
#   ${PROJECT_FOLDER}/temp/   -> 存储分片上传临时文件
# 示例:
#   Docker环境: /app/data/
#   Windows本地: E:/Project/easyCloudPan/storage/
#   Linux本地: /data/easyCloudPan/
PROJECT_FOLDER=E:/Project/easyCloudPan/backend
LOG_ROOT_LEVEL=info
ADMIN_EMAILS=admin@example.com
DEV_MODE=false
# Local-only: expose captcha code in /api/checkCode response header for automation (default false).
CAPTCHA_DEBUG_HEADER=false

# ------------------------------------------
# 邮件配置 (SMTP) [必改 - 用于注册验证码]
# ------------------------------------------
# 如需使用注册功能，请配置真实的邮件服务器
# QQ邮箱示例：
#   SPRING_MAIL_USERNAME=你的QQ号@qq.com
#   SPRING_MAIL_PASSWORD=QQ邮箱授权码（不是QQ密码）
# 获取QQ邮箱授权码：设置 -> 账户 -> POP3/SMTP服务 -> 生成授权码
SPRING_MAIL_HOST=smtp.qq.com
SPRING_MAIL_PORT=465
SPRING_MAIL_USERNAME=your_email@qq.com
SPRING_MAIL_PASSWORD=your_mail_password

# ------------------------------------------
# QQ 登录配置 [可选 - 用于QQ登录功能]
# ------------------------------------------
# 如需使用QQ登录，请到 https://connect.qq.com 申请
QQ_APP_ID=your_qq_app_id
QQ_APP_KEY=your_qq_app_key
QQ_URL_REDIRECT=http://localhost:8080/qqlogincallback

# ------------------------------------------
# Grafana 监控配置 [可选]
# ------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin123

# ------------------------------------------
# OAuth Multi-Platform Configuration [可选]
# ------------------------------------------
# 注意：Docker 环境下前端默认端口为 8080。
# 所有的 Redirect URI (回调地址) 必须与你在第三方平台配置的完全一致。

# 1. GitHub (https://github.com/settings/developers)
#    - Application name: EasyCloudPan (或任意名称)
#    - Homepage URL: http://localhost:8080 (你的部署地址)
#    - Authorization callback URL: http://localhost:8080/oauth/callback/github
OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=
OAUTH_GITHUB_REDIRECT_URI=http://localhost:8080/oauth/callback/github

# 2. Gitee (https://gitee.com/oauth/applications)
#    - 应用名称: EasyCloudPan
#    - 应用主页: http://localhost:8080
#    - 应用回调地址: http://localhost:8080/oauth/callback/gitee
OAUTH_GITEE_CLIENT_ID=
OAUTH_GITEE_CLIENT_SECRET=
OAUTH_GITEE_REDIRECT_URI=http://localhost:8080/oauth/callback/gitee

# 3. Google (https://console.cloud.google.com/apis/credentials)
#    - 授权的重定向 URI: http://localhost:8080/oauth/callback/google
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=
OAUTH_GOOGLE_REDIRECT_URI=http://localhost:8080/oauth/callback/google

# 4. Microsoft (Azure AD)
#    - 重定向 URI (Web): http://localhost:8080/oauth/callback/microsoft
OAUTH_MICROSOFT_CLIENT_ID=
OAUTH_MICROSOFT_CLIENT_SECRET=
OAUTH_MICROSOFT_REDIRECT_URI=http://localhost:8080/oauth/callback/microsoft
