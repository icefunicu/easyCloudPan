# ==========================================
# EasyCloudPan 环境配置
# ==========================================
# 复制此文件为 .env 并根据需要调整值。
# 该文件为配置模板，请复制为 .env 文件并根据需要修改。

# ------------------------------------------
# 数据库配置 (PostgreSQL)
# ------------------------------------------
POSTGRES_DB=easypan
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456

# ------------------------------------------
# Redis 配置
# ------------------------------------------
REDIS_PASSWORD=password123

# ------------------------------------------
# MinIO 配置 (对象存储)
# ------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadminpassword
MINIO_BUCKET=easypan
# 后端使用的 Bucket 名称
MINIO_BUCKET_NAME=easypan

# ------------------------------------------
# 后端应用配置
# ------------------------------------------
# 服务端口 (Docker 内部端口，通常不需要修改，除非更改映射)
SERVER_PORT=7090

# 项目文件夹 (容器内的存储路径)
PROJECT_FOLDER=/data/easypan/

# 日志级别 (debug, info, warn, error)
LOG_ROOT_LEVEL=info

# 管理员邮箱 (逗号分隔)
ADMIN_EMAILS=admin@example.com

# 开发模式 (true/false)
DEV_MODE=false

# ------------------------------------------
# 邮件配置 (SMTP)
# ------------------------------------------
SPRING_MAIL_HOST=smtp.qq.com
SPRING_MAIL_PORT=465
SPRING_MAIL_USERNAME=2041487752@qq.com
SPRING_MAIL_PASSWORD=dummy_mail_password

# ------------------------------------------
# QQ 登录配置
# ------------------------------------------
QQ_APP_ID=dummy_app_id
QQ_APP_KEY=dummy_app_key
QQ_URL_REDIRECT=http://localhost:8080/qqlogincallback

# ------------------------------------------
# 前端配置 (仅开发模式)
# ------------------------------------------
# VITE_API_TARGET=http://localhost:7090
