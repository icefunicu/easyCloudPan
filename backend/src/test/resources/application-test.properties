# Test Configuration
server.port=0

# Mocked or Dummy Values for Required Placeholders
QQ_APP_ID=test_app_id
QQ_APP_KEY=test_app_key
SPRING_MAIL_PASSWORD=test_mail_password
PROJECT_FOLDER=target/test-files/

# Datasource (Optional: could use H2 here if added to pom, for now keeping as is or using invalid valid to see if it connects)
# For context loading we just need properties to resolve.
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/easypan_test
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
